const frames=[{file:'Platform.png',time:'21:30',cells:4,tracks:2},{file:'Platform2.png',time:'23:24',cells:15,tracks:14}];
document.querySelectorAll('[data-frame]').forEach(button=>button.addEventListener('click',()=>{const f=frames[Number(button.dataset.frame)];const image=document.getElementById('radar-frame');image.src=`assets/${f.file}`;image.alt=`Radar dashboard at ${f.time} UTC showing ${f.cells} detected cells and ${f.tracks} active tracks`;document.getElementById('full-image').href=image.src;document.getElementById('frame-caption').textContent=`${f.time} UTC · ${f.cells} cells · ${f.tracks} active tracks`;document.querySelectorAll('[data-frame]').forEach(b=>{const selected=b===button;b.classList.toggle('active',selected);b.setAttribute('aria-pressed',String(selected))});}));
